<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MawiCompass.DeclinationInfoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Declination"
    BackgroundColor="#0F1115">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="14">
            <Label
                FontSize="22"
                Text="Magnetic vs True North"
                TextColor="#F3F4F6" />
            <Label Text="• Your phone’s compass measures Magnetic North (Earth’s field). • True North = Magnetic North + Declination. • Declination is positive ( + ) when magnetic north is east of true north, negative ( − ) when west." TextColor="#E5E7EB" />

            <Label
                Margin="0,10,0,0"
                FontSize="18"
                Text="How to use in this app"
                TextColor="#F3F4F6" />
            <Label Text="1) Toggle True North on to apply declination. 2) Enter your local declination in degrees (east = +, west = −). 3) The needle and numeric heading update immediately." TextColor="#E5E7EB" />

            <HorizontalStackLayout Margin="0,14,0,0" Spacing="12">
                <Button
                    BackgroundColor="#1A1D24"
                    Clicked="OpenDeclinationMap"
                    Text="Open Declination Map"
                    TextColor="#E5E7EB" />
                <Button
                    BackgroundColor="#1A1D24"
                    Clicked="OpenNoaa"
                    Text="NOAA WMM Calculator"
                    TextColor="#E5E7EB" />
            </HorizontalStackLayout>

            <Label
                Margin="0,10,0,0"
                Text="Tip: you can cache a value for the region you travel in most; declination changes slowly over years."
                TextColor="#9CA3AF" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
